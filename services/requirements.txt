# ============ 抠图服务 (remove_bg_service.py) ============
# 核心: 使用 briaai/RMBG-2.0 ONNX
onnxruntime>=1.16.0
numpy>=1.24.0
pillow>=10.0.0
# 模型文件已存在于 models/RMBG-2.0/onnx/

# ============ 检测裁切服务 (detect_crop_service.py) ============
# 核心: 使用 YOLOv8-Nano
ultralytics>=8.0.0
# 模型将自动下载: yolov8n.pt

# ============ 通用 ============
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
requests>=2.31.0
httpx>=0.25.0
python-multipart>=0.0.6
aiohttp>=3.9.0

# ============ 业务中台编排 (orchestration) ============
langgraph>=0.2.0
pillow>=10.0.0

# ============ 本地 VLM (Qwen2.5-VL-7B) ============
transformers>=4.49.0
qwen-vl-utils>=0.0.8
accelerate>=0.26.0
torch>=2.0.0
torchvision>=0.15.0
